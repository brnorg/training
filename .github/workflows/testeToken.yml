name: teste token

on: [workflow_call]

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: teste
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.tokenbrn }}
