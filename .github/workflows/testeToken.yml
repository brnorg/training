name: teste token

on: 
  workflow_dispatch:
    inputs:
        brntoken:
        description: 'token'
        required: true
        type: string      
  

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: teste
        uses: actions/checkout@v4
        with:
          token: ${{ inputs.brntoken }}
